services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "5050:5050"

  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    depends_on:
      - server
